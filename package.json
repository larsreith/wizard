{
  "name": "wizard",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "ftp-deploy": "^1.2.2",
    "react": "^16.2.0",
    "react-dom": "^16.2.0",
    "react-scripts": "1.1.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "ftp-deploy": "node ftp-deploy",
    "deploy": "react-scripts build; node ftp-deploy"
  }
}
